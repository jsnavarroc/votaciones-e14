requests>=2.31
openpyxl>=3.1
pymupdf>=1.24
Pillow>=10.0
questionary>=2.0
curl_cffi>=0.7  # impersona TLS de Chrome - evita bloqueos de Akamai Bot Manager

# Solo si vas a empaquetar a .exe:
# pyinstaller>=6.0
